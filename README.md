# Template for Node Tasks with TypeScript

## Available Scripts

`npm run build` ➜ building the application

`npm run start` ➜ build & run the application

`npm run dev` ➜ run the application without building
