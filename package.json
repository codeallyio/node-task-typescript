{
  "name": "node-task-ts",
  "version": "1.0.0",
  "description": "",
  "main": "server.ts",
  "scripts": {
    "build": "tsc",
    "start": "tsc && node build/src/server.js",
    "dev": "ts-node-dev --respawn --transpile-only src/server.ts",
    "test": "mocha -r ts-node/register test/*.ts --timeout 10000 --exit --reporter mocha-simple-html-reporter --reporter-options output=test-report.html"
  },
  "author": "Strove",
  "license": "ISC",
  "dependencies": {
    "@types/express": "^4.17.8",
    "express": "^4.17.1",
    "ts-node-dev": "^1.0.0",
    "typescript": "^4.0.3"
  },
  "devDependencies": {
    "@types/chai": "^4.2.14",
    "@types/mocha": "^8.0.3",
    "chai": "^4.2.0",
    "chai-http": "^4.3.0",
    "mocha": "^8.2.0",
    "mocha-simple-html-reporter": "^1.1.0",
    "ts-node": "^9.0.0"
  }
}
